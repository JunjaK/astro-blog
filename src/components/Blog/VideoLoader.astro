---
import { getBasePathWithUrl } from '@/utils/getBasePathWithUrl.ts';
import { Zorn } from '@minpluto/zorn';

interface Props {
  src: string;
}

const { src } = Astro.props;
---

<Zorn
  PlayerName="junjak"
  Video={getBasePathWithUrl(src)}
  ShowBackAndForward
  ShowPlaybackRate
  BigPlayButton
  Settings={[]}
/>
