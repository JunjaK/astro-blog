---
type Props = {
  src?: string;
  alt?: string;
  class?: string;
  width?: number;
  height?: number;
};

const { src, alt = 'blog-image', class: className, width, height } = Astro.props;
---

<img
  src={src ?? ''}
  alt={alt ?? 'blog-image'}
  loading="lazy"
  onerror={`this.src='/fallbackImg.svg'`}
  class={className}
  width={width}
  height={height}
/>
